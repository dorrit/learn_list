
  <!-- # <%= render :partial => 'layouts/errors', :locals => {:object => @tool} %> -->

<%= form_for @learning_list do |f| %>

 <%= f.label :section %>
  <%= f.collection_select(:section_id, @sections, :id, :section_name, :include_blank => true) if @sections %>
  <!-- # <%= link_to "Add Subject", new_subject_path, :class => "btn btn-mini" %>

  # <%= f.label :name %>
  # <%= f.text_field :name, :autofocus => true %>

  # <%= f.label :location %>
  # <%= f.text_field :location, :placeholder =>"http://" %>

  # <%= f.label :subject %>
  # <%= f.collection_select(:subject_id, @subjects, :id, :category, :include_blank => true) if @subjects %>
  # <%= link_to "Add Subject", new_subject_path, :class => "btn btn-mini" %>


  # <%= f.label :type %>
  # <%= f.collection_select(:type_id, @types, :id, :format, :include_blank => true) if @types %>
  # <%= link_to "Add Type", new_type_path, :class => "btn btn-mini" %>
        

  # <%= f.label :level %>
  # <%= f.collection_select(:level, Tool::LEVEL, :to_s, :to_s, :include_blank => true) %>

  # <%= f.label :cost %>
  # <%= f.collection_select(:cost, Tool::COST, :to_s, :to_s, :include_blank => true) %>


  # <%= f.submit "Save", :class => 'btn btn-primary' %>

  # <% end %> -->